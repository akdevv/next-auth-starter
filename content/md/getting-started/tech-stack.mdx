---
title: Tech Stack
description: Overview of the modern technologies powering Next Auth Starter
tags: [tech-stack, technologies, frameworks, libraries]
---

# 🛠️ Tech Stack Overview

Next Auth Starter is built with a carefully selected stack of modern, production-ready technologies that prioritize developer experience, performance, and scalability.

## 🎯 Core Framework

### ⚡ Next.js 15
**The React Framework for Production**

```ts
// App Router with Server Components
export default function Page() {
  return <div>Server-rendered by default</div>
}
```

**Why Next.js?**
- **Server-Side Rendering (SSR)** for better SEO and performance
- **App Router** with React Server Components
- **Built-in optimizations** for images, fonts, and scripts
- **Edge Runtime** support for global deployment
- **TypeScript** first-class support

### 🔷 TypeScript
**Type-Safe JavaScript at Scale**

```ts
// Full type safety across the entire application
interface User {
  id: string
  email: string
  emailVerified: boolean
  twoFactorEnabled: boolean
}
```

**Benefits:**
- **Compile-time error detection** prevents runtime bugs
- **Enhanced developer experience** with IntelliSense
- **Refactoring confidence** with automatic type checking
- **Self-documenting code** through type definitions

## 🎨 UI & Styling

### 🎭 shadcn/ui
**Beautiful, Accessible React Components**

```tsx
import { Button } from "@/components/ui/button"
import { Card } from "@/components/ui/card"

// Pre-built, customizable components
<Button variant="default" size="lg">
  Get Started
</Button>
```

**Features:**
- **Radix UI primitives** for accessibility
- **Tailwind CSS** for styling
- **Copy-paste** component philosophy
- **Fully customizable** design system

### 🎨 Tailwind CSS
**Utility-First CSS Framework**

```css
/* Responsive, utility-based styling */
<div className="bg-white dark:bg-gray-900 p-6 rounded-lg shadow-lg">
  <h1 className="text-2xl font-bold text-gray-900 dark:text-white">
    Welcome
  </h1>
</div>
```

**Advantages:**
- **Rapid prototyping** with utility classes
- **Consistent spacing** and sizing
- **Dark mode** support out of the box
- **Minimal bundle size** with purging

## 🔐 Authentication

### 🛡️ NextAuth.js v5
**Complete Authentication Solution**

```ts
// Flexible authentication configuration
export const authConfig = {
  providers: [
    Google({
      clientId: process.env.GOOGLE_CLIENT_ID,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET,
    }),
    Credentials({
      // Custom credentials logic
    })
  ]
}
```

**Features:**
- **Multiple providers** (OAuth, credentials, magic links)
- **Secure session management** with JWTs or database sessions
- **Built-in CSRF protection** and security headers
- **Serverless ready** for edge deployment

## 🗄️ Database & ORM

### 🐘 PostgreSQL (Supabase)
**Powerful, Open-Source Database**

```sql
-- Example schema with relations
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) UNIQUE NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);
```

**Why PostgreSQL?**
- **ACID compliance** for data integrity
- **Advanced features** like JSON support, full-text search
- **Excellent performance** and scalability
- **Strong ecosystem** and community support

### 🔷 Prisma ORM
**Next-Generation Database Toolkit**

```ts
// Type-safe database queries
const user = await prisma.user.create({
  data: {
    email: "user@example.com",
    emailVerified: false,
  },
  include: {
    sessions: true,
    twoFactorTokens: true,
  }
})
```

**Benefits:**
- **Type-safe queries** generated from schema
- **Auto-completion** and IntelliSense
- **Database migrations** management
- **Visual database browser** with Prisma Studio

## 📦 Package Management

### 🥟 Bun
**Fast All-in-One JavaScript Runtime**

```bash
# Lightning-fast package installation
bun install        # ~2x faster than npm
bun dev           # Built-in bundler and runner
bun test          # Integrated test runner
```

**Why Bun?**
- **Faster installs** compared to npm/yarn
- **Built-in bundler** and transpiler
- **Native TypeScript** support
- **Drop-in replacement** for Node.js

## 📧 Email Services

### 📮 Resend
**Modern Email API for Developers**

```tsx
// React email templates
import { Button, Html, Text } from '@react-email/components'

export function WelcomeEmail({ name }: { name: string }) {
  return (
    <Html>
      <Text>Welcome {name}!</Text>
      <Button href="https://example.com">Get Started</Button>
    </Html>
  )
}
```

**Features:**
- **React-based** email templates
- **High deliverability** rates
- **Analytics and tracking** built-in
- **Developer-friendly** API

## 🔒 Security & Authentication

### 🛡️ Security Features

```ts
// Built-in security measures
const securityHeaders = {
  'X-Frame-Options': 'DENY',
  'X-Content-Type-Options': 'nosniff',
  'Referrer-Policy': 'origin-when-cross-origin',
  'Permissions-Policy': 'camera=(), microphone=(), geolocation=()'
}
```

**Security Stack:**
- **bcrypt** for password hashing
- **speakeasy** for TOTP 2FA implementation
- **Rate limiting** with upstash/ratelimit
- **CSRF tokens** and secure sessions
- **Input validation** with zod schemas

## 🎯 Development Tools

### 🔧 Developer Experience

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build", 
    "lint": "next lint",
    "type-check": "tsc --noEmit"
  }
}
```

**Tools Included:**
- **ESLint** for code linting
- **Prettier** for code formatting
- **TypeScript** for type checking
- **Husky** for git hooks (optional)

## 📊 Performance & Monitoring

### ⚡ Built-in Optimizations

- **Image optimization** with Next.js Image component
- **Font optimization** with next/font
- **Bundle analysis** tools
- **Lighthouse** performance scoring
- **Core Web Vitals** monitoring

## 🚀 Deployment Ready

### 🌐 Platform Agnostic

```bash
# Deploy to any platform
vercel deploy     # Vercel (recommended)
netlify deploy    # Netlify
docker build .    # Docker containers
```

**Deployment Options:**
- **Vercel** (optimal Next.js support)
- **Netlify** for static deployments  
- **Docker** for containerized deployment
- **AWS/GCP/Azure** with serverless functions

## 🔄 Why This Stack?

This tech stack was chosen for:

1. **Developer Experience** - Fast development with great tooling
2. **Performance** - Optimized for speed and efficiency  
3. **Scalability** - Handles growth from prototype to production
4. **Security** - Built-in security best practices
5. **Maintainability** - Type safety and clear architecture
6. **Community** - Strong ecosystem and support

Ready to explore how it all fits together? Check out the [Project Structure](/docs/getting-started/project-structure) guide! 